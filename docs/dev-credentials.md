# بيانات دخول تجريبية — محلي فقط

> لا ترفعي كلمات المرور الحقيقية للإنتاج هنا.  
> **موجة A:** ممنوع تشغيل `db:seed` على سيرفر إنتاج/مشترك — غيّري كلمات المرور فوراً إن وُجدت.

| الدور | البريد | كلمة المرور |
|--------|--------|-------------|
| مدير الأستوديو | `admin@alray.studio` | `Admin@123456` |
| مصور (موجة B) | `photographer@alray.studio` | `Photo@123456` |

الرابط: http://localhost:3000/admin/login

```bash
cd apps/web
npm run db:seed
```

بعد موجة B: سجّلي خروجاً وأعيدي الدخول حتى تتحدّث الجلسة بالدور.
