# بيانات دخول تجريبية — المرحلة 2 (محلي فقط)

> لا ترفعي كلمات المرور الحقيقية للإنتاج هنا.  
> **موجة A:** ممنوع تشغيل `db:seed` على سيرفر إنتاج/مشترك بكلمة `Admin@123456` — غيّري كلمة الأدمن فوراً إن وُجدت.

| البند | القيمة |
|--------|--------|
| الرابط | http://localhost:3000/admin/login |
| البريد | `admin@alray.studio` |
| كلمة المرور | `Admin@123456` |

إعادة إنشاء الحساب (محلي فقط):
```bash
cd apps/web
npm run db:seed
```
