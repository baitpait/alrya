---
description: كشف بداية/نهاية جلسة العمل — واتساب تقرير عمل للإدارة فقط
alwaysApply: true
---

# دورة حياة الجلسة — بداية / نهاية

الهدف: تسجيل **تقرير عمل للإدارة على واتساب** (ماذا اشتغلنا · ماذا أُنجز · بداية · نهاية · مدة).  
التواصل الودّي مع نهلة يبقى **في Cursor فقط** — لا قوالب واتساب للمبرمجة عند بداية/نهاية الجلسة.

## مصدران للكشف (كلاهما صحيح)

### أ) تقني (تلقائي)
- فتح محادثة/وكيل جديد → hook `sessionStart` → `session-start --to mgmt`
- توقف الوكيل / إغلاق الجلسة → hook `stop` → `session-end --to mgmt`
- الأداة تمنع التكرار خلال دقائق (`SESSION_DEDUPE_MS`)

### ب) لغوي (من كلام المستخدم — إلزامي على الوكيل)
إذا ظهرت نية واضحة في الرسالة، نفّذ الأمر حتى لو الـ hook اشتغل أو لم يشتغل (الـ dedupe يحمي من التكرار).

#### إشارات **بداية جلسة** → نفّذ فوراً:
```bash
node tools/pm-notify.mjs session-start --to mgmt --worked-on "<محور العمل باختصار>"
```
أمثلة كلمات/عبارات (عربية أو إنجليزية):
- بداية جلسة / نبدأ / يلا نشتغل / افتح الجلسة
- صباح الخير / مساء الخير (مع طلب عمل أو «نبدأ»)
- وكيل جديد / محادثة جديدة / جلسة جديدة / session start
- السلام عليكم + طلب عمل في **أول رسالة** بالمحادثة
- let's start / new agent / start session / good morning (مع طلب عمل)

في **أول رد** في الشات: جملة ودّية قصيرة لنـهلة (صباح/مساء الخير) ثم المتابعة التقنية — **بدون** واتساب تحفيزي لها.

#### إشارات **نهاية جلسة** → نفّذ فوراً:
```bash
node tools/pm-notify.mjs session-end --to mgmt \
  --worked-on "<على ماذا اشتغلنا>" \
  --done "<ماذا أُنجز فعلياً>"
```
أمثلة:
- نهاية جلسة / خلّصنا / أنهينا / أوقف / وقف الوكيل
- شكراً وانتهينا / إلى اللقاء / باي للعمل اليوم
- session end / stop agent / we're done / good night (بعد عمل)

في **آخر رد** في الشات: شكر بشري قصير لنـهلة. واتساب = تقرير للإدارة فقط (بداية/نهاية/مدة/منجز).

## ممنوع
- إرسال قوالب واتساب ثابتة للمبرمجة عند بداية/نهاية الجلسة
- تجاهل إشارة نهاية واضحة دون `session-end` للإدارة
- وضع ملخص إداري فارغ أو معلّب بدل ما أُنجز فعلاً في الجلسة (عند الإغلاق اللغوي)
- رسائل واتساب مكررة يدوياً إذا قال الطرفية أن الإرسال تُخطّي بسبب dedupe — اكتفِ بالرد في الشات

## إن لم تتضح النية
لا ترسل إشعار جلسة. اسأل بجملة واحدة فقط عند الشك القوي: «هل نعتبر هذه بداية جلسة رسمية أم متابعة داخل الجلسة؟»
